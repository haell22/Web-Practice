const loginpage = document.querySelector(".main-box");
const loginbutton = document.querySelector("#login-button");

loginbutton.addEventListener("click", (event) => {
    loginpage.style.display = "none";
});